package am.techshop.common.enums;

/**
 * The customer-facing language TechShop AM communications (order notifications, emails)
 * should be written in. hy is the platform default - the same fallback the frontend's
 * LanguageService uses when no language has been selected yet.
 */
public enum Language {
    HY,
    EN,
    RU
}
